
#include<bits/stdc++.h>
using namespace std;

class Node
{
public:
    int val;
    Node* next;
    Node(int val)
    {
        this->val=val;
        this->next=NULL;
    }
};
void print_linked_list(Node* head)
{
    int freq[101]={0};
    Node* temp=head;
    int flag=0;
    while(temp!=NULL)
    {
        cout<<temp->val<<" ";
        freq[temp->val]++;
        if(freq[temp->val]>1)
        {
            flag=1;
        }
        temp=temp->next;
    }
     if(flag==1)
     {
         cout<<"YES";

     }
     else
     {
         cout<<"NO";
     }
}
 int main()
 {
     Node* head=new Node(2);
     Node* a=new Node(1);
     Node* b=new Node(5);
     Node* c=new Node(3);
     Node* d=new Node(4);
     Node* e=new Node(8);
     Node* f=new Node(9);
     head->next=a;
     a->next=b;
     b->next=c;
     c->next=d;
     d->next=e;
     e->next=f;
    print_linked_list(head);

 }
